---
- name: Create GlusterFS Volumes
  import_tasks: volume/create-volumes.yml
  tags: create
  when: ( volume is defined and volume == true ) and inventory_hostname in groups['control']


- name: Delete GlusterFS Volumes
  import_tasks: volume/delete-volumes.yml
  tags: delete
  when: ( volume is defined and volume == true ) and inventory_hostname in groups['control']


- name: Delete GlusterFS Volumes Quota
  import_tasks: volume/enable-quota.yml
  tags: enable
  when: ( quota is defined and quota == true ) and inventory_hostname in groups['control']

